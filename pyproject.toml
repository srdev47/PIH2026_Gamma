[project]
name = "fake-job-detector"
version = "0.1.0"
description = "Fake job detection app"
readme = "README.md"
requires-python = ">=3.11,<3.13"

dependencies = [
    "streamlit",
    "pandas",
    "scikit-learn",
    "joblib"
]